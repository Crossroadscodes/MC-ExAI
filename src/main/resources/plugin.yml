name: ExAI
version: '${project.version}'
main: com.exai.ExAI
softdepend:
  - Vault
commands:
  exai:
    description: ExAI插件主命令
    usage: /exai <reload|opengui|question|import|help>
permissions:
  exai.op:
    description: Allows access to ExAI review/admin features
    default: op
